# -----------------------------
# Dotfiles: ignore all by default…
# -----------------------------
.*
# …but keep core project dotfolders/files:
!.git*
!.vscode/
!.github/
!.clangd
!.env.example   # document env vars; see .env below

# -----------------------------
# VS Code: keep portable config, ignore caches
# -----------------------------
.vscode/*
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
# Known noisy caches from extensions
.vscode/ipch/
.vscode/**/binaries/
.vscode/**/browse.*
.vscode/**/targets

# Local environment (user/machine specific)
.env

# -----------------------------
# Build outputs & artifacts
# -----------------------------
/build/
/dist/
plugin.*.map

# Linker/obj/debug cruft (anywhere)
*.exp
*.lib
*.pdb
*.ilk
*.obj

# Platform libs/binaries (anywhere)
*.dll
*.so
*.dylib
*.exe

# -----------------------------
# Editor / OS noise
# -----------------------------
.DS_Store
Thumbs.db

# -----------------------------
# Don’t commit SDKs or installed trees
# -----------------------------
Rack-SDK/
Rack-SDK-*/
plugins-*/

# If you ever vendor dependencies here, ignore the cache:
# /dep/